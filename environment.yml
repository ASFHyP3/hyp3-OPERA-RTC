name: hyp3-opera-rtc
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python>=3.9
  - pip
  # For running
  - gdal
  - numpy
  - shapely
  - requests
  - urllib3
  - lxml
  # For packaging, and testing
  - setuptools
  - setuptools_scm
  - wheel
  - pytest
  - pytest-console-scripts
  - pytest-cov
